name: Main build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Build
        run: ./gradlew build
      - name: upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: Build Jar
          path: build/libs/pipo_project-0.0.1-SNAPSHOT-plain.jar